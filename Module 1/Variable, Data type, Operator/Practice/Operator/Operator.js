let inputWidth;
let inputHeight;
inputWidth = prompt("Enter the width of the rectangle: ");
inputHeight = prompt("Enter the height of the rectangle: ");

let area = inputWidth * inputHeight;
document.write("The area of the rectangle is: " + area);